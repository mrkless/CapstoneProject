specVersion: "v1beta"
serviceId: "admin_panel"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "admin_panel-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
